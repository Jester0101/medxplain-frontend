This directory holds the trained model artifact (risk_model.joblib + risk_model.meta.json)
produced by: python -m medicalbigdata.serving.train

It is intentionally gitignored -- nothing here is committed until you train a real model
on the LURIC dataset. See backend/SERVING.md.
