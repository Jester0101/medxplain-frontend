# Minimal, CPU-only dependencies for the risk-model training + serving pipeline
# (medicalbigdata.serving.*). Works fine on macOS -- unlike requirements.txt,
# which is for the full research stack (vLLM/DeepSpeed/Unsloth, CUDA-only).
#
# Install with:  pip install -r requirements-serving.txt

fastapi
uvicorn[standard]
pydantic
catboost
scikit-learn
shap
joblib
pandas
numpy
pyreadstat
httpx
pytest
